{
  "name": "@mxenabled/web-widget-sdk",
  "version": "1.0.0",
  "description": "MX Web Widget SDK",
  "main": "dist/index.js",
  "scripts": {
    "format": "npm run prettier -- -w",
    "prettier": "prettier src example",
    "clean": "[ -d dist ] && rm -r dist || true",
    "build": "npm run clean && npm run build:amd && npm run build:system",
    "build:amd": "echo Building AMD; tsc --declaration --module amd --outFile dist/amd/mx-web-widget-sdk.js",
    "build:system": "echo Building SystemJS; tsc --declaration --module system --outFile dist/system/mx-web-widget-sdk.js"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.mx.com:Sean.Brown/mx-web-widget-sdk.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.6.2"
  },
  "prettier": {
    "printWidth": 100,
    "semi": false,
    "trailingComma": "all",
    "singleQuote": false
  }
}
